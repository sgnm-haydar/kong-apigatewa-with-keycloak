_format_version: "2.1"

services:
  - name: facilityHistory
    url: https://localhost:3001/facilityHistory
    routes:
      - name: facilityHistory
        paths:
          - /facilityHistory


plugins:
  - name: rate-limiting
    service: facilityHistory
    config:
      minute: 5
      policy: local